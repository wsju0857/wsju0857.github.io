export const baseFontSize = {
    xs: 11,
    sm: 12.5,
    md: 14,
    lg: 15.5,
    xl: 15.5
};

export const breakpointBarHeight = {
    xs: (baseFontSize.xs - 8) * 9,
    sm: (baseFontSize.sm - 8) * 9,
    md: (baseFontSize.md - 8) * 9,
    lg: (baseFontSize.lg - 8) * 9,
    xl: (baseFontSize.xl - 8) * 9
};

export default {
    baseFontSize,
    breakpointBarHeight
};
